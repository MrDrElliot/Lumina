#include "MaterialCompiler.h"

namespace Lumina
{
    uint32 FMaterialCompiler::Add(uint32 A, uint32 B)
    {
        return 0;
    }

    uint32 FMaterialCompiler::Subtract(uint32 A, uint32 B)
    {
        return 0;
    }

    uint32 FMaterialCompiler::Multiply(uint32 A, uint32 B)
    {
        return 0;
    }

    uint32 FMaterialCompiler::Divide(uint32 A, uint32 B)
    {
        return 0;
    }
}
